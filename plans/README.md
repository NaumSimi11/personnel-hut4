# Implementation Plans

Generated by the improve skill on 2026-09-10, at commit `a690d31`. Execute in
the order below unless dependencies say otherwise. Each executor: read the plan
fully before starting, honor its STOP conditions, and update your row when done
(unless a reviewer dispatched you and maintains this index).

Scope of this round (per maintainer): `prototype/` and `docs/` only. The root
`index.html` / `hr-system.html` app was explicitly out of scope and was not audited.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Playwright smoke-test baseline (walkthroughs A & B) | P1 | M | — | DONE (reviewed; branch `advisor/001-verification-baseline` @ `50640c4`) |
| 002 | Fix double render on navigation + robustness gaps | P1 | S | 001 | DONE (reviewed; branch `advisor/002-navigation-and-robustness` @ `564d4f6`) |
| 003 | Escape company names consistently in HTML templates | P2 | S | 002 | DONE (reviewed after one revision — plan's exception list was incomplete, corrected in plan text; branch `advisor/003-consistent-html-escaping` @ `2e186c1`) |
| 004 | Unified "needs a decision" queue on Overview | P2 | S | 003 | DONE (reviewed; branch `advisor/004-needs-action-queue` @ `3161e8c`) |
| 005 | "View as" permission simulation with capability gating | P1 | M | 004 | DONE (reviewed; branch `advisor/005-view-as-simulation` @ `c37d71b`) |
| 006 | Employee self-service overview in view-as mode | P2 | M | 005 | DONE (reviewed; one documented test-sketch deviation accepted; branch `advisor/006-employee-self-service` @ `a587cfc`) |
| 007 | Read-only Projects panel + projects.view capability | P2 | M | 006 | DONE (reviewed; branch `advisor/007-projects-read-only-panel` @ `667d3b9` — contains the whole round, 12/12 tests green; merge is the maintainer's call) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Branch strategy

The advisor never commits to `main`. Each executor works in an isolated git
worktree and branches from the previous plan's branch:
`advisor/001-verification-baseline` ← `advisor/002-navigation-and-robustness` ← … ←
`advisor/007-projects-read-only-panel`. The final branch contains the whole
round; merging it into `main` is the maintainer's decision.

## Round 03 — app build (one agent at a time, per maintainer)

| Plan | Title | Status |
|------|-------|--------|
| 008 | Records depth: invite↔record attach + private details | DONE (reviewed, merged to main @ `482301c`; full suite 6/6) |
| 009 | Hiring workspace A: requests + approve/reject/changes | DONE (reviewed, merged to main @ `9221d24`; full suite 7/7 after one cold-start flake) |
| 010 | Hiring workspace B: jobs, candidates, confirm hire (RPC 0009) | DONE (reviewed, merged to main @ `9e2f529`; full suite 8/8) |
| 011 | Onboarding: queue, plan detail, task actions, readiness | DONE (reviewed, merged to main @ `1831765`; full suite 9/9) |
| 012 | Home: needs-action queue, metrics, my tasks | DONE (reviewed, merged to main @ `412d1a3`; reviewer then switched the post-login landing to it; full suite 10/10) |
| 013 | Self-service: My workspace (profile, onboarding, tasks, access, projects) | DONE (reviewed, merged to main @ `3839087`; full suite 11/11) |
| 014 | Company profiles: holding list + per-company tabs | DONE (reviewed, merged to main @ `8082763`; full suite 12/12) |
| 015 | Zoho Projects read-only sync (client, mapping, CLI, docs) | DONE (reviewed, merged to main @ `53ce3ab`; server 23/23, app 12/12; live HTTP calls await credentials) |
| — | Offboarding foundation: departure template + `schedule_departure` / `complete_departure` RPCs (migration 0010) | DONE (main @ `66201fa`; smoke tests) |
| — | Company management: create / edit / archive with full profile (brand + logo bucket, legal & registration, contacts; migration 0011) | DONE (reviewed, main @ `10407ee`; full suite 13/13) |
| 016 | Offboarding UI: schedule departure, queue, kind-aware plan page, mark as former; `auth.can()` | DONE (reviewed, main @ `8dd3764`; full suite 14/14) |
| 017 | Job workspace: five-step journey, Description + screening questions, Channels (careers publish, manual records, out-of-date), Promotion state machine (`advance_promotion`), Activity trail (migration 0012) | DONE (reviewed, main @ `2c3ac5c`; full suite 15/15) |
| 018a | Candidate page: private files (`application_files` + `candidate-files` bucket, signed links), screening answers, timeline + notes, decision panel (owner / next action / due), reasoned reject & withdraw (migration 0013) | DONE (reviewed, main @ `614d433`; full suite 16/16) |
| 018b | Interviews + panel, blind scorecards (RLS), scorecard criteria per job, offer state machine (`advance_offer`, immutable author, separated approval), Confirm hire from the accepted offer, job Interviews & Offer tab (migration 0014) | DONE (reviewed, main @ `b3c93bb`; full suite 17/17 ×2) |
| 019 | Careers page + application intake: public per-company listing and form with CV upload, spam protection | TODO |
| 020 | AI assist: CV summary with references, screening digest, JD and promo drafts — server-side, human-reviewed, never a score | TODO |
| 021 | Recruitment reporting: funnel, time-to-hire, source attribution | TODO |
| 022 | Employment changes: scheduled job/manager/location changes, circular-reporting guard, departments & locations management | TODO |

Round-03 executors branch from `main` as `feature/NNN-<slug>`; the reviewer
runs the full E2E suite serially and merges after review.

## Dependency notes

- Everything depends on 001: the Playwright suite is the verification gate in every later plan.
- 002→003→004→005 are chained because they all edit `prototype/app.js` and each plan's "current state" assumes the previous diffs.
- 006 and 007 consume plan 005's `viewAs`/`can()` helpers; 007 also appends a card to 006's `selfServiceOverview()`.
- 007 bumps the localStorage seed version to v2 (discards stored demo data — authorized by `docs/design-round-01.md:54`).

## Findings considered and rejected / deferred

- **localStorage-only persistence, admin-only non-enforcement, manual task completion, single sample request per company, "Ready for day one" with the intro task open** — all documented design decisions in `docs/design-round-01.md`, not defects.
- **Full-re-render architecture** — appropriate at 40 records for a design prototype; not worth replacing.
- **app.js restructuring into modules / immutable state** (audit finding DEBT-01) — deferred: real cost, but doing it mid-round would invalidate every plan's excerpts; revisit after this round lands, with the test suite as the safety net.
- **Focus loss on re-render + tabs keyboard pattern** (audit finding A11Y-01) — deferred to the next design round; needs a focus-restoration approach designed alongside the render model, not bolted on.
- **`btn()` not escaping its text parameter** — all call sites are author-controlled and some intentionally embed HTML; documented in plan 003 as out of scope.
- **Blueprint's richer status lists (Screening/Withdrawn/Rejected)** — documented slice reduction, not drift.
